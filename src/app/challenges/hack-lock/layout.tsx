export const metadata = {
  title: "Pattern Lock Challenge | Vitaly Ford",
  description: "Can you crack the pattern lock?",
};

export default function HackLockLayout({ children }: { children: React.ReactNode }) {
  return children;
}
